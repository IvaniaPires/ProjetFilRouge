<!DOCTYPE html>
<html lang="fr">

<head>
    <link rel="icon" href="/assets/img/editePaint.png">
    <meta charset="utf-8">
    <title>Mangez-Chez-Vous</title>
    <link rel="stylesheet" href="/css/styles.css" />

</head>

<body>
    <header style="background-image:url(/assets/img/<%=delivery_mens[0].img_mcv_local%>)">
    </header>
    <h1>Additionner livreur</h1>
    <% if(Object.values(delivery_mens[0]).length===2) {%>
        <h3>Il n'y aucan restaurant pour additionner</h3>
        <%} else {%>
            <ul>
                <%delivery_mens.forEach(element => {%>
                    <% if(element.created_delivery_man === 0) {%>
                    <li>
                        <a class="available_list" href="/new_delivery_man/<%=element.id_delivery_man_application%>/<%=element.id_mcv_local%>">
                            Nom: <%=element.lastname_delivery_man%>, Prénom: 
                                <%=element.firstname_delivery_man%>, Contact téléphonique:
                                        <%=element.phone_delivery_man%>, Email:
                                            <%=element.mail_delivery_man%>
                        </a>
                    </li>
                    <%}
                });%>
            </ul>
            <%}%>
                <div class=div_button>
                    <a class="button" href="/stores/<%=delivery_mens[0].id_mcv_local%>">
                        <span>Retour</span>
                    </a>
                </div>
</body>

</html>